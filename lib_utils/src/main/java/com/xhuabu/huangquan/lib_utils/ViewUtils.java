package com.xhuabu.huangquan.lib_utils;

import android.view.View;
import android.widget.LinearLayout;

/**
 * Created by hugo on 2016/5/12.
 */
public class ViewUtils {

/*    *//**
     * 默认高度 0.5dp
     * @param colorId
     * @param marginInDp
     * @return
     *//*
    @Deprecated
    public static View createGrayLine(int colorId, int marginInDp) {
        View grayLine = new View(UIUtils.getContext());
        grayLine.setBackgroundResource(colorId);

        LinearLayout.LayoutParams grayLineParams = new LinearLayout.LayoutParams(-1, DpUtils.viewDp2Px(0.5f));
        grayLineParams.leftMargin = grayLineParams.rightMargin = DpUtils.viewDp2Px(marginInDp);
        return grayLine;
    }*/
}
